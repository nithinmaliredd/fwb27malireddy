extends layout

block content
  h1= title
  p Welcome to #{title} Js
  P Nithin Malireddy is  learning how to use pug templates.
  button(type="button", class="btn btn-success", onclick="update()") Press Me for the contents
  div(id="target") Display
  
  script. 
    let client_count = 0;
    function update() {
      client_count = client_count + 1;
      document.getElementById("target").innerHTML = `Value now ${client_count}`  ;
      }