extends layout

block content
  h1= title
  p Welcome to #{title} Js
  P Nithin Malireddy is  learning how to use pug templates to dynamically generate web pages on the server side.
  button(type="button", class="btn btn-success", onclick="update()") Press Me
  br
  br
  div(id = "target") 
    b Display
  
  script.  
   var client_count=0;
     function update() {
       client_count++;
       document.getElementById("target").innerHTML=`Value now ${client_count}`;
     }
  